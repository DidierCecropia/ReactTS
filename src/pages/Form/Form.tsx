import { Container, Row } from "react-bootstrap";

const FormComponent = () => {
  return (
    <Container>
      <Row>Should display a Chart but the API doesn't provide any data.</Row>
    </Container>
  );
};

export default FormComponent;
